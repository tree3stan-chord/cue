[build-system]
requires = [
  "setuptools>=42",
  "wheel",
  "numpy"
]
build-backend = "setuptools.build_meta"

[project]
name = "gesture-dsp"
version = "0.1.0"
dependencies = [
  "numpy",
  "opencv-python",
  "pyaudio"
]